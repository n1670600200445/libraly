<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เข้าสู่ระบบ</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@2.1.4/css/boxicons.min.css">

    <style>
        /* ตั้งค่าให้หน้าเว็บโหลดพร้อมกับแสดงเอฟเฟกต์ */
        html, body { height: 100%; }

        body {
            background: linear-gradient(135deg, #ffccbc 40%, #fbe9e7 60%);
            font-family: 'Arial', sans-serif;
            animation: fadeIn 0.8s ease-in forwards;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* การ์ดเข้าสู่ระบบแบบ Glassmorphism */
        .form-signin {
            max-width: 550px;
            padding: 3rem 3rem; /* ลด padding แนวตั้งให้ไม่เยอะเกินไป */
            background: rgba(255, 138, 101, 0.85);
            backdrop-filter: blur(12px);
            border-radius: 15px;
            box-shadow: 0 0 25px rgba(255, 138, 101, 0.5);
            border: 2px solid rgba(255, 255, 255, 0.4);
            text-align: center;
            transition: box-shadow 0.3s ease-in-out;
        }

        .form-signin:hover {
            box-shadow: 0 0 25px rgba(255, 87, 34, 0.8);
        }

        /* เพิ่มเงาให้ input เวลาโฟกัส */
        .form-control:focus {
            box-shadow: 0 0 10px #bf360c;
            border-color: #bf360c;
        }

        .form-floating {
            position: relative;
        }

        .form-floating input {
            padding-right: 40px;
        }

        .form-floating i {
            position: absolute;
            top: 50%;
            right: 15px;
            transform: translateY(-50%);
            font-size: 20px;
            color: #555;
            transition: color 0.3s, transform 0.3s;
        }

        .form-floating input:focus ~ i {
            color: #ff5722;
            transform: scale(1.2);
        }

        /* ปรับปุ่มให้มี Gradient และ Animation */
        .btn-primary {
            background: linear-gradient(135deg, #ff5722, #e64a19);
            border: none;
            transition: all 0.3s ease-in-out;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, #ff8a65, #ffab91);
            transform: scale(1.05);
        }

        /* ปรับโลโก้ให้ดูน่าสนใจ */
        .logo-img {
            width: 100px;
            height: auto;
            display: block;
            margin: 0 auto 15px;
            filter: drop-shadow(0px 0px 10px #ffccbc);
            transition: transform 0.3s ease-in-out;
        }

        .logo-img:hover {
            transform: scale(1.1);
        }
    </style>
</head>
<body>

    <main class="form-signin">
        
        <div class="text-center">
            <img src="/images/summer-cpe.png" alt="Summer CPE Logo" class="logo-img">
        </div>
        <h2 class="text-center">โปรแกรมจัดหมวดหมู่หนังสือและ <br>การยืมคืนหนังสือ</h2>
        <form method="post" action="/loginpost">
            <h2 class="h3 mb-3 fw-normal text-center">เข้าสู่ระบบ</h2>
            
            <div class="form-floating">
                <input type="text" class="form-control" id="username" name="username" required>
                <label for="username">ชื่อผู้ใช้</label>
                <i class='bx bxs-user'></i>
            </div>

            <div class="form-floating mt-2">
                <input type="password" class="form-control" id="password" name="password" required>
                <label for="password">รหัสผ่าน</label>
                <i class='bx bxs-lock'></i>
            </div>

            <button class="btn btn-primary w-100 py-2 mt-3" type="submit">
                เข้าสู่ระบบ
            </button>
            
            <p class="mt-3 text-center">
                ยังไม่มีบัญชี? <a href="/apply">สมัครสมาชิก</a>
            </p>
        </form>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
