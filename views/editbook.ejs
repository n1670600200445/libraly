<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }
        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
        }
        .form-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .form-group label {
            font-weight: bold;
        }
        .btn-custom {
            background-color: #6c757d; /* สีเทา */
            border: none;
            color: white;
        }
        .btn-custom:hover {
            background-color: #5a6268; /* สีเทาเข้มเมื่อ hover */
        }
        .btn-primary {
            background-color: #f57c00; /* สีส้ม */
            border: none;
            color: white;
        }
        .btn-primary:hover {
            background-color: #e65c00; /* สีส้มเข้มเมื่อ hover */
        }
        .btn-container {
            display: flex;
            justify-content: space-between;
        }
        .btn-container .btn {
            width: 48%;
        }
        footer {
            background-color: #ea9292;
            padding: 10px;
            text-align: center;
            color: white;
            margin-top: 30px;
        }
    
        /* ทำให้ input, select, textarea มีขนาดและสไตล์เท่ากัน */
        .form-control, 
        .form-select, 
        textarea {
            display: block;
            width: 100%;
            padding: 0.5rem 1.25rem;
            font-size: 1rem;
            line-height: 1.5;
            color: #495057;
            background-color: #ffffff;
            background-clip: padding-box;
            border: 1px solid #ced4da;
            border-radius: 0.375rem;
            transition: border-color 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }
    
        /* ขณะ focus */
        .form-control:focus, 
        .form-select:focus, 
        textarea:focus {
            box-shadow: 0 0 10px #bf360c;
            border-color: #bf360c;
            outline: none;
        }
    
        /* เพิ่มให้ select ตัวใหญ่ขึ้น */
        .form-select-lg {
            font-size: 1.1rem;
        }
    
        .form-label {
            font-weight: bold;
            color: #333333;
            margin-bottom: 0.5rem;
        }
    
        .invalid-feedback {
            display: block;
            font-size: 0.875rem;
            color: #dc3545;
        }
    </style>
    
</head>
<body>

<div class="container form-container">
    <h1>แก้ไขข้อมูลหนังสือ</h1>

    <form action="/book/update/<%= book.id %>" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
            <label for="title" class="form-label">ชื่อหนังสือ</label>
            <input type="text" id="title" name="title" class="form-control" value="<%= book.title %>" required>
        </div>
    
        <div class="mb-3">
            <label for="author" class="form-label">ผู้แต่ง</label>
            <input type="text" id="author" name="author" class="form-control" value="<%= book.author %>" required>
        </div>
    
        <div class="mb-3">
            <label for="category" class="form-label">ชื่อหมวดหมู่</label>
            <br>
            <select id="category" name="category" class="form-select form-select-lg" required>
                <% categories.forEach(category => { %>
                    <option value="<%= category.id %>" <% if (category.id === book.category_id) { %>selected<% } %>><%= category.name %></option>
                <% }) %>
            </select>
            <!-- <div class="invalid-feedback">
                กรุณาเลือกหมวดหมู่
            </div> -->
        </div>
    
        <div class="mb-3">
            <label for="year" class="form-label">ปีที่พิมพ์</label>
            <input type="number" id="year" name="year" class="form-control" value="<%= book.year %>" required>
        </div>
    
        <div class="mb-3">
            <label for="isbn" class="form-label">รหัส ISBN ของหนังสือ</label>
            <input type="text" id="isbn" name="isbn" class="form-control" value="<%= book.isbn %>" required>
        </div>

        <div class="form-group">
            <label for="story">เรื่องย่อ:</label>
            <textarea id="story" name="story" class="form-control" rows="5" required><%= book.story %></textarea>
          </div>

          <div class="form-group">
            <label for="quantity">จำนวน:</label>
            <input type="number" id="quantity" name="quantity" class="form-control" value="<%= book.quantity %>" required>
        </div>
          
        <div class="form-group">
            <label for="image">อัปโหลดภาพหนังสือ</label>
            <input type="file" name="image" id="image" class="form-control-file">
            
            <% if (book.image) { %>
                <small class="form-text text-muted">ไฟล์เดิม: <%= book.image %></small>
            <% } %>
        </div>        
            
        <div class="form-group btn-container">
            <a href="/book" class="btn btn-custom">ยกเลิก</a>
            <button type="submit" class="btn btn-primary">บันทึกการแก้ไข</button>
        </div>
    </form>
    
<!-- Bootstrap JS and dependencies -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>

<!-- <footer>
    <p>&copy; 2025 Your Bookstore</p>
</footer> -->

</body>
</html>
