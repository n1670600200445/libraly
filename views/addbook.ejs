<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Book</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <style>
        body {
            background-color: #fff; /* Pastel orange background */
            font-family: 'Arial', sans-serif;
        }
        h1 {
            text-align: center;
            color: #ff6f00; /* Pastel orange text for header */
            margin-bottom: 30px;
        }
        .form-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .form-group label {
            font-weight: bold;
            color: #ff6f00; /* Pastel orange label */
        }
        .btn-primary, .btn-custom {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            font-weight: bold;
        }
        .btn-custom {
            background-color: #ffab73; /* Soft pastel orange for the custom button */
            border: none;
            color: white;
        }
        .btn-custom:hover {
            background-color: #ff8c42; /* Slightly darker on hover */
        }
        .btn-primary {
            background-color: #ff6f00; /* Pastel orange for submit button */
            border: none;
            color: white;
        }
        .btn-primary:hover {
            background-color: #e65c00; /* Slightly darker on hover */
        }
        .btn-container {
            display: flex;
            justify-content: space-between;
        }
        .btn-container .btn {
            width: 48%; /* Ensures both buttons take almost equal space */
        }
        footer {
            background-color: #ff6f00; /* Pastel orange footer */
            padding: 10px;
            text-align: center;
            color: white;
            margin-top: 30px;
        }
        .form-control:focus {
            box-shadow: 0 0 10px #bf360c;
            border-color: #bf360c;
        }
    </style>
</head>
<body>

<div class="container">


    <div class="form-container">
        <form action="/book" method="POST" enctype="multipart/form-data">
            <h1>เพิ่มหนังสือ</h1>
            <!-- <div class="form-group">
                <label for="s">แถว</label>
                <select class="form-control" name="category_id">
                    <option value="">กรุณาเลือก</option>
                    <% if(categories && categories.length> 0){ %>
                        <% for(var i=0; i < categories.length; i++){ %>
                            <option value="<%= categories[i].id %>">
                                <%= categories[i].code %>
                            </option>

                            <% } %>
                                <% } %>
                </select>
            </div> -->

            <div class="form-group">
                <label for="title">ชื่อหนังสือ:</label>
                <input type="text" id="title" name="title" class="form-control" required>
            </div>

            <div class="form-group">
                <label for="author">ผู้แต่ง:</label>
                <input type="text" id="author" name="author" class="form-control" required>
            </div>

            <div class="form-group">
                <label for="s">หมวดหมู่</label>
                <select class="form-control" name="category_id">
                    <option value="">กรุณาเลือก</option>
                    <% if(categories && categories.length> 0){ %>
                        <% for(var i=0; i < categories.length; i++){ %>
                            <option value="<%= categories[i].id %>">
                                <%= categories[i].name %>
                            </option>

                            <% } %>
                                <% } %>
                </select>
            </div>

            <div class="form-group">
                <label for="year">ปีที่พิมพ์:</label>
                <input type="number" id="year" name="year" class="form-control" min="0" max="3000" required>
            </div>

            <div class="form-group">
                <label for="isbn">รหัส ISBN ของหนังสือ:</label>
                <input type="text" id="isbn" name="isbn" class="form-control" required pattern="[0-9]+" title="กรุณากรอกเฉพาะตัวเลข">
            </div>
            
            <div class="form-group">
                <label for="story">เรื่องย่อ:</label>
                <textarea id="story" name="story" class="form-control" rows="5" required></textarea>
                <small class="text-danger">หากไม่มีเรื่องย่อ กรุณาใส่เครื่องหมาย -</small>
              </div>
              
              <div class="form-group">
                <label for="quantity">จำนวน:</label>
                <input type="number" id="quantity" name="quantity" class="form-control" min="1" max="1000" required>
            </div>

            <div class="form-group">
                <label for="image">อัปโหลดภาพหนังสือ</label>
                <input type="file" name="image" id="image" class="form-control-file">
            </div>

            <div class="form-group btn-container">
                <a href="/book" class="btn btn-custom">ย้อนกลับ</a>
                <button type="submit" class="btn btn-primary">เพิ่มข้อมูล</button>  
            </div>
        </form>
    </div>
</div>

<!-- Bootstrap JS and dependencies -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>

<!-- <footer>
    <p>&copy; 2025 ระบบห้องสมุดออนไลน์</p>
</footer> -->

</body>
</html>
