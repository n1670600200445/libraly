<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library System</title>
    <!-- DataTables CSS -->
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.1.2/css/boxicons.min.css" rel="stylesheet">
    <style>
        /* เปลี่ยนสีตัวหนังสือ CPE เป็นสีส้ม */
        header .d-inline-flex {
            font-size: 28px;
            font-weight: bold;
            color: #ff6f00 !important; /* สีส้ม */
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: color 0.3s, transform 0.3s;
        }

        header .d-inline-flex:hover {
            color: #ff9800 !important; /* สีส้มอ่อนเมื่อ hover */
            transform: scale(1.1);
        }

        /* เปลี่ยนสีตัวหนังสือในเมนู nav เป็นสีส้ม */
        .nav-link {
            color: #bf360c !important; /* สีส้ม */
            font-size: 16px;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        /* เปลี่ยนสีเมื่อ hover */
        .nav-link:hover {
            color: #ff9800 !important; /* สีส้มอ่อนเมื่อ hover */
        }

        .btn-logout {
            font-size: 16px;
            font-weight: bold;
            background: linear-gradient(135deg, #b0120a, #e84e40); /* Gradient from #b0120a to #e84e40 */
            border: none;
            color: white;
            padding: 10px 25px;
            border-radius: 5px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            display: inline-flex;
            align-items: center; /* Center the text and icon */
            transition: background 0.3s, transform 0.3s;
        }

        /* Hover effect for the logout button */
        .btn-logout:hover {
            background: linear-gradient(135deg, #e84e40, #b0120a); /* Inverted gradient on hover */
            transform: scale(1.05);
        }

        /* Styling the icon inside the button */
        .btn-logout i {
            margin-right: 8px; /* Add some space between text and icon */
            font-size: 20px; /* Make the icon a little larger */
        }

        header {
            padding: 20px;
            background-color: #f8f9fa;
            border-bottom: 2px solid #ddd;
        }

        /* Styling the entire nav container */
        .nav {
            margin-left: auto;
            margin-right: auto;
            text-align: center;
        }

        /* Styling the container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding-top: 20px;
        }

        /* Make sure the button has some space */
        .col-md-3.text-end {
            display: flex;
            align-items: center;
            justify-content: flex-end;
        }

        /* Flexbox styles to center the menu and logout button */
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
        }

        .nav {
            flex-grow: 1; /* Allow nav to take available space */
            justify-content: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="header-content">
                <div class="col-md-3 mb-2 mb-md-0">
                    <a class="d-inline-flex link-body-emphasis text-decoration-none">
                        CPE
                    </a>
                </div>

                <!-- Navigation menu -->
                <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
                    <li><a href="/book" class="nav-link px-2">จัดการหนังสือทั้งหมด</a></li>
                    <li><a href="/categories" class="nav-link px-2">จัดการหมวดหมู่หนังสือ</a></li>
                    <li><a href="/records" class="nav-link px-2">ยืมหนังสือ</a></li>
                    <li><a href="/borrow" class="nav-link px-2">ข้อมูลการยืมหนังสือ</a></li>
                    <li><a href="/users" class="nav-link px-2">จัดการบัญชีผู้ใช้</a></li>
                </ul>

                <!-- Logout button -->
                <div class="col-md-3 text-end">
                    <a href="/logout" class="btn btn-logout">
                        <i class="bx bx-exit"></i>ออกจากระบบ
                    </a>
                </div>
            </div>
        </header>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- DataTables JS -->
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

